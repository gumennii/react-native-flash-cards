import {
  QUIZ_START,
  QUESTION_ADD,
  QUESTION_ANSWERED_CORRECT,
  QUESTION_ANSWERED_INCORRECT,
  DECK_CREATE
}

function decks (state = {}, action) => {
  switch(action) {
    default: return state
  }
}
