import React, { PropTypes } from 'react'
import { View, Text } from 'react-native'

export const Quiz = (props) => {
  return (
    <View>
      <Text>Quiz</Text>
    </View>
  );
}


export default Quiz
